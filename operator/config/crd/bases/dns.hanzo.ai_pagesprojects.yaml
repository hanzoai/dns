---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: pagesprojects.dns.hanzo.ai
spec:
  group: dns.hanzo.ai
  names:
    kind: PagesProject
    listKind: PagesProjectList
    plural: pagesprojects
    singular: pagesproject
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.customDomain
      name: Domain
      type: string
    - jsonPath: .status.subdomain
      name: Subdomain
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PagesProject is a Cloudflare Pages site deployed and domain-wired
          by the operator.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              PagesProjectSpec defines a Cloudflare Pages site managed declaratively.
              It composes the DNS layer: when CustomDomain+ZoneRef are set the operator
              emits a CNAME DnsRecord pointing the domain at the project's pages.dev host.
            properties:
              accountId:
                description: AccountID is the Cloudflare account that owns the project.
                type: string
              customDomain:
                description: CustomDomain to attach to the project (e.g. "hanzo.bot").
                type: string
              productionBranch:
                default: main
                description: ProductionBranch for git-connected projects.
                type: string
              projectName:
                description: ProjectName is the Cloudflare Pages project name. Defaults
                  to metadata.name.
                type: string
              zoneRef:
                description: |-
                  ZoneRef is the DnsZone that owns CustomDomain, used to emit the CNAME.
                  Required when CustomDomain is set.
                type: string
            required:
            - accountId
            type: object
          status:
            description: PagesProjectStatus reports the observed state of the Pages
              project.
            properties:
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              phase:
                description: Phase represents the current lifecycle phase.
                enum:
                - Pending
                - Ready
                - Error
                type: string
              subdomain:
                description: Subdomain is the project's canonical *.pages.dev host.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
