name: Build and Push to GHCR (disabled — native pipeline)
# The canonical deploy pipeline is .hanzo/workflows/deploy.yml:
# Hanzo Git push → act_runner (in-cluster) → BuildKit builds ./Dockerfile →
# ghcr.io/hanzoai/dns:<sha> → kubectl patch app/dns → operator reconcile.
# GitHub is a mirror; image build/push no longer happens here.
on:
  workflow_dispatch:
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "native pipeline is .hanzo/workflows/deploy.yml; GitHub is a mirror"
